services:
  - type: web
    name: virtual-india-ai
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
    startCommand: gunicorn internship_project.wsgi:application

